import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seventhpath.habittracker',
  appName: 'Seventh Path',
  webDir: 'dist'
};

export default config;
