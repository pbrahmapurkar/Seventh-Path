import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seventhpath.habittracker',
  appName: 'Seventh Path: Habit Tracker',
  webDir: 'dist',
  appVersion: '1.0.7'
};

export default config;
